# Mobile app (Phase 2)

Phase 2 will contain the Flutter iOS application. It is intentionally empty in Phase 1.

The app talks only to the REST API documented in `../README.md` (`/v1/generations`, `/v1/capabilities`, `/v1/uploads/presign`).
